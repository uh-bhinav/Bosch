# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Conda / micromamba (local only — use environment.yml)
.micromamba/
.conda/
conda-meta/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Secrets / local config
.env
.streamlit/secrets.toml

# Claude Code — local overrides only (settings.json IS committed)
.claude/settings.local.json

# Logs
*.log
# Generated mold-half STEP exports (Milestone 1.11)
output/mold_halves/
# User-uploaded STEP files (Phase F2) — never data/parts/, which stays the
# curated, read-only fixture set.
data/uploads/

# pytest-cov output — regenerate locally with `pytest --cov`, don't track
.coverage
reports/coverage_html/
